{
  "version": "V26.1",
  "purpose": "GIS Store Studio V26.1: stable camera focus and custom-boundary publication rendering",
  "reference_version": "V4.2",
  "files": [
    "README.md",
    "StudioAnalysis.jsx",
    "StudioBoundaryUpload.jsx",
    "StudioLayers.jsx",
    "StudioLayout.jsx",
    "StudioMap.jsx",
    "StudioPage.jsx",
    "StudioUpload.jsx",
    "analysis/idw.js",
    "backend-boundaries/README.md",
    "backend-boundaries/boundaryResolver.js",
    "backend-boundaries/server-route-example.js",
    "boundaryApi.js",
    "package.studio-deps.json",
    "server/gis/boundaries/README.txt",
    "spatial.js",
    "studio.css"
  ],
  "syntax_checked": false,
  "notes": [
    "Location-map camera uses the uploaded CSV point extent as the authoritative automatic viewport.",
    "Asynchronous permanent boundary resolution redraws boundaries without changing the automatic CSV viewport.",
    "Clicking a project CSV layer explicitly fits all features in that layer; clicking a project boundary explicitly fits that boundary.",
    "Custom boundary upload requests an explicit boundary focus but never expands an unrelated boundary to include outlying CSV points.",
    "Map camera fitting uses bounded padding and a 16.5 maximum zoom so compact point datasets remain readable while all points stay visible.",
    "A4 location layout uses boundary plus points in the main frame only when every sampling point is inside the boundary; otherwise it keeps the point-focused main frame and boundary overview.",
    "The custom boundary remains a visible project layer with its delete control.",
    "The permanent boundary API and existing remove-boundary implementation are unchanged.",
    "Custom boundary is drawn in the A4 main map when it contains every sampling point; otherwise it is shown only in Boundary overview.",
    "Automatic boundary-resolution updates no longer trigger a camera fit.",
    "Explicit project-layer focus resolves the clicked layer by ID and fits only that layer."
  ],
  "name": "camera-boundary-layer-focus-worker-fix",
  "changes": [
    "Removed the undefined focusPoints reference that caused the Studio page to crash.",
    "Separated automatic camera fitting from explicit project-layer/boundary focus actions.",
    "Project layer clicks now perform a direct camera fit without requiring a remount.",
    "Automatic boundary loading never changes the CSV point viewport.",
    "Custom boundary focus includes the boundary and active sampling points.",
    "Added explicit Vite MapLibre worker URL configuration to prevent the worker from being served as HTML.",
    "Location layout keeps point-only extent for unrelated custom boundaries and uses boundary + points when all points are inside.",
    "Fixed custom boundary visibility in the A4 main layout when all sampling points are inside.",
    "Prevented automatic boundary data changes from re-zooming the map canvas.",
    "Strengthened project-layer click-to-zoom handling and removed a duplicate focus-state update."
  ]
}
